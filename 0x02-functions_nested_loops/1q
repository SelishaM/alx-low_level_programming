#include "main.h"

/**
 * add - add two numbers from input
 * @a: first parameter
 * @b: second parameter
 *
 * Description: add two numbers
 * Result: Always (0).
 */

int add(int a, int b)
{
	return (a + b);
}
